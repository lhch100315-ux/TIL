# JWT (JSON Web Token)

두 당사자 간에 정보를 JSON 객체 형태로 안전하게 전송하기 위한, 간결하고 URL-safe한 토큰 기반 인증 방식이다.

---

## 특징
- 서명을 통해 데이터의 **무결성(변조 여부)**을 검증할 수 있음  
- 주로 **인증(Authentication)** 및 **인가(Authorization)**에 사용됨  
- **Stateless(무상태)**: 서버가 세션을 저장하지 않고, 토큰 자체에 정보를 담아 처리 → 확장성 좋음  
- **Base64 인코딩**: 암호화가 아니라 단순 인코딩이므로 민감 정보 저장하면 안 됨  

---

## 구조 (3부분)
`Header.Payload.Signature` (점 `.`으로 구분)

### 1. Header
- 토큰 타입 (JWT)
- 서명에 사용할 해시 알고리즘 (예: HS256)

### 2. Payload
- 사용자 정보 (클레임, Claims)
- 예: 사용자 ID, 권한(role), 만료 시간(exp)

### 3. Signature
- Header + Payload를 비밀키로 서명한 값  
- 토큰이 **위변조 되었는지 확인**하는 역할  

---

## 한 줄 핵심 정리
**JWT는 서버가 상태를 저장하지 않고, 토큰 자체로 사용자 인증과 권한을 처리하는 서명 기반 토큰이다**
