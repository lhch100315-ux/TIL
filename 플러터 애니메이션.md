# 플러터의 2가지 애니메이션

## 애니메이션의 종류

- 암묵적 애니메이션 (Implicit Animation)
- 명시적 애니메이션 (Explicit Animation)

---

# 1. 암묵적 애니메이션 (Implicit Animation)

- 애니메이션이 이미 프로그래밍되어 있거나 스타일이 입혀져 있다.
- 애니메이션의 트리거, 즉 애니메이션을 시작하는 타이밍을 지정할 필요가 없다.
- 값이 변함에 따라 자동으로 애니메이션이 일어난다.

### 예시

**투명도가 변하는 애니메이션**

투명도를 의미하는 변수의 값이 `50`에서 `100`으로 변하는 순간, 자동으로 애니메이션이 시작된다.

---

# 2. 명시적 애니메이션 (Explicit Animation)

암묵적 애니메이션 위젯에서는 제공하지 않는 애니메이션 커스터마이징을 할 수 있다.

- 애니메이션을 컨트롤하기 위해 `AnimationController`를 등록해야 한다.
- 반드시 애니메이션 트리거를 지정해줘야 한다.

---

# 3. 명시적 애니메이션의 종류

## ① 빌트인 위젯을 사용하는 방법

`Transition`으로 끝나는 위젯을 사용하면 된다.

예시:

- `FadeTransition`
- `ScaleTransition`
- `RotationTransition`
- `SlideTransition`

## ② `AnimatedBuilder`를 사용하는 방법

이 방식을 사용하면 원하는 위젯을 `AnimatedBuilder`로 감싸는 식으로 구현하게 된다.

---

# 4. 필수 세팅

명시적 애니메이션을 사용하기 위해서는 크게 다음 두 가지가 필요하다.

- 컨트롤러: `AnimationController`
- 애니메이션: `Animation`

---

# 4-1. 컨트롤러: `AnimationController`

`AnimationController`는 애니메이션을 직접 제어하는 역할을 한다.

### 주요 역할

- 애니메이션을 시작한다.
- 애니메이션을 거꾸로 재생한다.
- 애니메이션을 멈춘다.
- 주어진 `value`로 애니메이션을 조절한다.
- 애니메이션의 `lowerBound`와 `upperBound`를 설정한다.
- 설정한 애니메이션의 몇 %까지 진행할지 결정한다.
- 애니메이션의 초기값을 설정할 수 있다.

---

## 필수 파라미터

| 이름 | 정체 | 넘겨준 객체 |
|---|---|---|
| `vsync` | `TickerProvider`를 제공한다. 1프레임마다 주어진 콜백 함수를 실행한다. | `this` (위젯의 `State`에 `TickerProviderStateMixin`을 사용했기 때문에 자기 자신을 넘겨도 된다.) |

---

# 4-2. 애니메이션: `Animation`

### 컨트롤러와의 관계

컨트롤러가 애니메이션의 상태를 조절한다.

즉,

- 상태는 `Animation` 객체가 들고 다닌다.
- `AnimationController`가 그 값을 변경할 수 있다.

### `Animation` 클래스

- `Animation` 클래스는 `abstract class`이다.
- `Tween`, `CurvedAnimation`, `TrainHoppingAnimation` 등을 사용해 객체를 만들 수 있다.
- 공식 문서 예제에서는 `CurvedAnimation`을 사용했다.

---

## `CurvedAnimation`

`CurvedAnimation`은 애니메이션이 어떤 흐름으로 진행될지 정한다.

직접 보는 것이 훨씬 이해가 빠르다.

### 주요 파라미터

| 이름 | 정체 | 넘겨준 객체 |
|---|---|---|
| `parent` | `AnimationController` 여기에 `addStatusListener`를 달아서 변화를 감지한다. | 위에서 만들어준 `animationController` 객체. |
| `curve` | 애니메이션이 어떤 흐름으로 진행될지 정한다. | 원하는 `Curve` 객체. |

### 예시

```dart
final animation = CurvedAnimation(
  parent: animationController,
  curve: Curves.easeInOut,
);