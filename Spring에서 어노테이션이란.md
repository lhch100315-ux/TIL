# Spring에서 어노테이션이란

- 코드에 추가 정보를 붙여서 Spring이 동작을 자동으로 처리하도록 만드는 기능
- 클래스, 메서드, 필드 등에 붙여서 설정을 대신함

---

## 주요 어노테이션

### 1. @Component
- Spring이 관리하는 객체(빈)로 등록

### 2. @Service
- 비즈니스 로직을 담당하는 클래스에 사용

### 3. @Repository
- 데이터 접근 계층(DB 관련)에 사용

### 4. @Controller
- 사용자 요청을 처리하는 클래스에 사용

### 5. @Autowired
- 필요한 객체를 자동으로 주입

### 6. @RestController
- JSON 형태로 데이터를 반환하는 컨트롤러

### 7. @RequestMapping
- 요청 URL을 매핑

---
