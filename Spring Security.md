## 1. Spring Security
**Spring 기반 애플리케이션의 인증(Authentication)과 인가(Authorization)를 담당하는 보안 프레임워크이다.**

로그인, 권한 체크, 세션 관리, 비밀번호 암호화 등 보안 관련 기능을 제공한다.

---

## 2. 인증 (Authentication)
**사용자가 누구인지 확인하는 과정이다.**

예시
- 로그인 (아이디 / 비밀번호)
- 토큰 기반 인증 (JWT)
- 소셜 로그인

Spring Security에서는 사용자의 아이디와 비밀번호를 확인하여 해당 사용자가 실제 존재하는지 검증한다.

---

## 3. 인가 (Authorization)
**인증된 사용자가 어떤 권한을 가지고 있는지 확인하는 과정이다.**

예시
- 관리자 페이지 접근 제한
- 특정 API 접근 권한 체크
- 게시글 수정 권한 (작성자만 가능)

Spring Security에서는 사용자 권한(Role)에 따라 접근 가능한 기능을 제한한다.

---

## 4. 동작 흐름

1. 사용자가 요청을 보낸다  
2. Spring Security가 요청을 가로챈다 (Filter)  
3. 인증 진행 (로그인 여부 확인)  
4. 인증 성공 시 사용자 정보 저장  
5. 인가 진행 (권한 확인)  
6. 조건이 맞으면 요청 처리, 아니면 차단  

---

## 5. 핵심 구성 요소

- Security Filter Chain  
  요청을 가로채서 인증과 인가를 처리하는 필터들의 집합

- UserDetailsService  
  사용자 정보를 불러오는 역할

- PasswordEncoder  
  비밀번호를 암호화하고 검증하는 역할

- SecurityContext  
  인증된 사용자 정보를 저장하는 공간

---

## 6. 특징

- 기본적으로 모든 요청을 보호함 (인증 필요)
- 다양한 인증 방식 지원 (세션, JWT 등)
- 권한 기반 접근 제어 가능
- 보안 관련 기능을 일관되게 관리 가능
