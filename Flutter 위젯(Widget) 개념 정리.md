# Flutter 위젯(Widget) 개념 정리

## 1. 위젯(Widget)

Flutter에서 화면을 구성하는 기본 단위이다.

Flutter에서는 화면에 보이는 거의 모든 요소를 위젯으로 표현한다.

예시:
- 텍스트
- 버튼
- 이미지
- 아이콘
- 입력창
- 여백
- 화면 전체

즉, Flutter 화면은 여러 위젯을 조합해서 만든다.

---

## 2. Widget Tree

Flutter 화면에 사용되는 위젯들의 부모-자식 관계를 나타내는 구조이다.

예를 들어 하나의 화면에 여러 위젯이 있다면 각 위젯이 계층적으로 연결되어 있다.

### 특징

- 위젯은 부모와 자식 관계를 가진다.
- 하나의 위젯이 다른 위젯을 포함할 수 있다.
- Flutter 화면은 Widget Tree 형태로 구성된다.

---

## 3. StatelessWidget

상태가 변경되지 않는 위젯이다.

화면의 값이 변경될 필요가 없는 경우 사용한다.

예시:
- 고정된 텍스트
- 단순한 아이콘
- 변경되지 않는 화면

### 특징

- 상태를 직접 관리하지 않는다.
- 주어진 값에 따라 화면을 구성한다.
- 비교적 단순한 UI에 사용한다.

---

## 4. StatefulWidget

상태가 변경될 수 있는 위젯이다.

예시:
- 버튼을 누르면 숫자가 증가하는 화면
- 체크박스
- 입력창
- 애니메이션
- 로그인 상태

StatefulWidget은 StatefulWidget과 State 객체로 구성된다.

### 특징

- 상태를 가질 수 있다.
- 상태가 변경되면 화면을 다시 그릴 수 있다.
- setState()를 통해 화면을 갱신할 수 있다.

---

## 5. State

StatefulWidget의 현재 상태를 저장하고 관리하는 객체이다.

예를 들어 카운터 앱에서 숫자 값이 현재 0인지 1인지 2인지 등을 State가 관리한다.

### 상태 변경 과정

상태 변경
→ setState()
→ build() 실행
→ 화면 갱신

---

## 6. setState()

StatefulWidget에서 상태가 변경되었음을 Flutter에 알리는 함수이다.

setState()가 실행되면 Flutter는 해당 위젯을 다시 빌드한다.

### 사용 목적

- 변수 값 변경
- 화면 갱신
- 사용자 행동에 따른 UI 변경

---

## 7. build()

화면을 구성할 위젯을 반환하는 메서드이다.

Flutter는 build()에서 반환된 위젯을 이용하여 화면을 구성한다.

StatelessWidget과 StatefulWidget 모두 build()를 사용한다.

---

## 8. MaterialApp

Flutter 앱의 전체적인 기본 설정을 담당하는 위젯이다.

### 주요 기능

- 앱의 기본 구조 설정
- 테마 설정
- 화면 이동 설정
- 앱의 기본 화면 설정

Flutter 앱의 최상위 위젯으로 자주 사용된다.

---

## 9. Scaffold

Flutter에서 기본적인 화면 구조를 제공하는 위젯이다.

### 주요 영역

- AppBar
- Body
- FloatingActionButton
- Drawer
- BottomNavigationBar

Flutter에서 하나의 화면을 만들 때 매우 자주 사용한다.

---

## 10. AppBar

화면 상단에 표시되는 앱 바이다.

### 주요 용도

- 화면 제목 표시
- 뒤로가기 버튼
- 메뉴 버튼
- 추가 기능 버튼

---

## 11. Body

Scaffold에서 실제 화면의 주요 내용을 표시하는 영역이다.

다양한 위젯을 넣어서 화면의 내용을 구성할 수 있다.

---

## 12. Container

자식 위젯을 담고 크기와 디자인을 설정할 수 있는 위젯이다.

### 주요 기능

- 크기 설정
- 배경색 설정
- 테두리 설정
- 모서리 둥글게 설정
- 여백 설정
- 정렬

---

## 13. Row

자식 위젯을 가로 방향으로 배치하는 위젯이다.

### 사용 예시

- 가로로 버튼 배치
- 아이콘과 텍스트 배치
- 여러 요소를 한 줄에 배치

---

## 14. Column

자식 위젯을 세로 방향으로 배치하는 위젯이다.

### 사용 예시

- 여러 텍스트를 세로로 배치
- 로그인 화면 구성
- 여러 버튼을 세로로 배치

---

## 15. Stack

여러 위젯을 겹쳐서 배치하는 위젯이다.

### 사용 예시

- 이미지 위에 텍스트 배치
- 이미지 위에 버튼 배치
- 위젯을 겹쳐서 디자인 구성

---

## 16. Text

화면에 텍스트를 표시하는 위젯이다.

### 주요 속성

- 글자 크기
- 글자 색상
- 글자 굵기
- 글자 정렬
- 폰트

---

## 17. Image

화면에 이미지를 표시하는 위젯이다.

### 이미지 종류

- 네트워크 이미지
- 프로젝트 내부 이미지
- 파일 이미지
- 메모리 이미지

---

## 18. Icon

아이콘을 표시하는 위젯이다.

### 사용 예시

- 홈
- 검색
- 설정
- 사람
- 삭제
- 뒤로가기

---

## 19. IconButton

아이콘을 버튼처럼 사용할 수 있도록 하는 위젯이다.

### 사용 예시

- 검색 버튼
- 설정 버튼
- 삭제 버튼
- 메뉴 버튼
- 뒤로가기 버튼

---

## 20. ElevatedButton

일반적인 버튼을 만들 때 사용하는 위젯이다.

사용자가 버튼을 눌렀을 때 특정 기능을 실행할 수 있다.

---

## 21. TextButton

텍스트 형태의 버튼이다.

배경이나 테두리가 강조되지 않은 버튼을 만들 때 사용한다.

### 사용 예시

- 취소
- 더 보기
- 로그인
- 회원가입

---

## 22. OutlinedButton

테두리가 있는 버튼이다.

강조된 버튼보다는 비교적 가벼운 느낌의 버튼을 만들 때 사용한다.

---

## 23. TextField

사용자가 텍스트를 입력할 수 있도록 하는 위젯이다.

### 사용 예시

- 아이디 입력
- 비밀번호 입력
- 이름 입력
- 검색어 입력
- 메시지 입력

---

## 24. Padding

자식 위젯 주변에 내부 여백을 추가하는 위젯이다.

주로 위젯과 다른 요소 사이의 공간을 조절할 때 사용한다.

---

## 25. SizedBox

일정한 크기나 공간을 만들 때 사용하는 위젯이다.

주로 위젯 사이에 일정한 간격을 만들 때 사용한다.

---

## 26. Center

자식 위젯을 가운데에 배치하는 위젯이다.

화면 중앙에 특정 위젯을 배치할 때 자주 사용한다.

---

## 27. Expanded

Row나 Column에서 남은 공간을 자식 위젯이 차지하도록 하는 위젯이다.

### 사용 목적

- 남은 공간 채우기
- 여러 위젯의 공간 비율 조절
- 화면 크기에 맞게 위젯 확장

---

## 28. ListView

여러 위젯을 스크롤 가능한 목록으로 표시하는 위젯이다.

### 사용 예시

- 게시글 목록
- 친구 목록
- 채팅 목록
- 상품 목록

---

## 29. GridView

위젯을 격자 형태로 배치하는 위젯이다.

### 사용 예시

- 사진 갤러리
- 상품 목록
- 게임 목록
- 아이콘 목록

---

## 30. GestureDetector

사용자의 터치 동작을 감지하는 위젯이다.

### 감지할 수 있는 동작

- 클릭
- 길게 누르기
- 드래그
- 두 번 누르기

---

## 31. InkWell

터치 효과와 클릭 이벤트를 구현할 때 사용하는 위젯이다.

커스텀 버튼이나 클릭 가능한 영역을 만들 때 자주 사용한다.

---

## 32. Dialog

화면 위에 팝업 형태로 추가 정보를 표시하는 위젯이다.

### 사용 예시

- 삭제 확인
- 경고 메시지
- 선택 메뉴
- 사용자 입력

---

## 33. Navigator

Flutter에서 화면 이동을 관리하는 기능이다.

### 주요 기능

- 새로운 화면으로 이동
- 이전 화면으로 돌아가기
- 화면 이동 기록 관리

---

# 34. child와 children

## child

하나의 자식 위젯을 받을 때 사용한다.

예시:
- Container
- Center
- Padding

## children

여러 개의 자식 위젯을 받을 때 사용한다.

예시:
- Row
- Column
- Stack
- ListView

---

# 35. 주요 위젯 정리

| 위젯 | 용도 |
|---|---|
| MaterialApp | 앱 전체 설정 |
| Scaffold | 기본 화면 구조 |
| AppBar | 상단 바 |
| Text | 텍스트 |
| Image | 이미지 |
| Icon | 아이콘 |
| Container | 크기 및 디자인 |
| Row | 가로 배치 |
| Column | 세로 배치 |
| Stack | 위젯 겹치기 |
| Center | 가운데 배치 |
| Padding | 내부 여백 |
| SizedBox | 크기 및 공간 |
| Expanded | 남은 공간 차지 |
| ListView | 스크롤 목록 |
| GridView | 격자 목록 |
| TextField | 텍스트 입력 |
| ElevatedButton | 기본 버튼 |
| TextButton | 텍스트 버튼 |
| OutlinedButton | 테두리 버튼 |
| IconButton | 아이콘 버튼 |
| GestureDetector | 터치 감지 |
| InkWell | 터치 효과 및 클릭 |
| Dialog | 팝업 |
| Navigator | 화면 이동 |

---

# 핵심 정리

Flutter
→ 위젯을 조합해서 앱의 UI를 만드는 프레임워크

Widget
→ 화면을 구성하는 기본 단위

Widget Tree
→ 위젯들의 부모-자식 관계

StatelessWidget
→ 상태가 변경되지 않는 위젯

StatefulWidget
→ 상태가 변경될 수 있는 위젯

State
→ StatefulWidget의 상태를 관리

setState()
→ 상태 변경 후 화면을 갱신

build()
→ 화면을 구성할 위젯을 반환

MaterialApp
→ 앱 전체 설정

Scaffold
→ 기본적인 화면 구조

Row
→ 가로 배치

Column
→ 세로 배치

Stack
→ 겹쳐서 배치

Container
→ 크기와 디자인 설정

ListView
→ 스크롤 가능한 목록

Navigator
→ 화면 이동