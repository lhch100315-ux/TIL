# Git & GitHub 개념 정리 (정확 버전)

## 1. Git이란?

- **분산 버전 관리 시스템 (DVCS)**
- 코드 변경 이력을 저장하고 관리
- 여러 사람이 동시에 작업해도 충돌 최소화

### 핵심 특징

- **버전 관리**: 언제든 이전 코드로 돌아갈 수 있음  
- **분산 구조**: 모든 개발자가 전체 저장소를 가지고 있음  
- **속도 빠름**: 로컬에서 대부분 작업 처리  

---

## 2. GitHub를 쓰는 이유

- Git을 사용하는 **원격 저장소 서비스**

### 사용하는 이유

- 협업 (여러 명이 동시에 작업)  
- 원격 백업 (코드 안전성 확보)  
- 코드 리뷰 (PR 기능)  
- 프로젝트 관리 (이슈, PR 등)  

디코 안 쓰는 이유  
→ 코드 관리 기능이 없음 (버전 관리, 충돌 해결, 이력 관리 등)

---

## 3. 로컬 vs 원격

구분 설명   
------------ 
\| 로컬(Local) \| 내 컴퓨터 \|  
\| 원격(Remote) \| GitHub 같은 서버 \|  

### 중요한 점

- 로컬에 올린다고 원격이 없어지는 거 아님  
- **로컬 + 원격 둘 다 존재 (동기화 구조)**  

---

## 4. Git 기본 흐름
작업 → add → commit → push

### 명령어 설명
git init  
 - 현재 폴더를 Git 저장소로 만듦 (처음 1번만)

git add .  
 - 변경된 파일을 스테이징 영역에 올림  

 git commit -m "메시지"  
- 변경사항을 하나의 버전으로 저장  
 
git push origin main  
- 로컬 커밋을 원격 저장소로 업로드  
 
git pull origin main  
- 원격 → 로컬로 가져오기  

---

## 5. Git 내부 구조 (중요)
작업폴더 → 스테이징 → 저장소(commit)


- add = 스테이징  
- commit = 버전 생성  
- push = 원격 전송  

"git이 임시저장 서버"라는 표현  
→ 틀림  
→ Git은 버전 관리 시스템이고, 서버는 GitHub  

---

## 6. 브랜치 전략 (Git Flow 개념)

### 기본 구조
main (배포용)
└ dev (개발 통합)
└ feature (기능 개발)


### 흐름

1\. feature 브랜치에서 기능 개발  
2\. dev 브랜치에 merge  
3\. 테스트 완료  
4\. main으로 merge → 배포  

### 핵심

- main에 바로 작업하면 위험함  
- 항상 dev → main 순서  

---

## 7. Pull Request (PR)

- 코드 합치기 전에 리뷰 요청하는 기능  

### 흐름

1\. feature → dev로 PR 요청  
2\. 팀원이 코드 리뷰  
3\. 문제 없으면 merge  

### 역할

- 코드 품질 관리  
- 협업 필수 기능  

---

## 8. 협업 구조

- 팀원 4명 → 로컬 4개 + 원격 1개  
- 각자 로컬에서 작업 후 push  
- 충돌 나면 merge로 해결  

---

## 9. 컨벤션 (Convention)

팀에서 정하는 규칙  
