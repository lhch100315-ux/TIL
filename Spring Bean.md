## 1. Bean
**Bean은 Spring이 생성하고 관리하는 객체이다.**

일반 자바에서는 개발자가 직접 객체를 생성하지만,  
Spring에서는 객체를 직접 생성하지 않고 Spring 컨테이너가 대신 생성하고 관리한다.  
이렇게 Spring이 관리하는 객체를 Bean이라고 한다.

---

## 2. IoC (Inversion of Control)
**객체의 생성과 관리의 주체가 개발자에서 Spring으로 넘어간 개념이다.**

기존 방식에서는 개발자가 객체를 생성하고 의존관계를 직접 연결한다.  
Spring에서는 객체 생성과 생명주기를 모두 컨테이너가 담당한다.

즉, 객체에 대한 제어권이 개발자가 아닌 Spring으로 이동한 상태를 IoC라고 한다.

---

## 3. DI (Dependency Injection)
**객체가 필요로 하는 의존성을 외부에서 주입해주는 방식이다.**

객체는 필요한 다른 객체를 직접 생성하지 않는다.  
대신 Spring이 필요한 Bean을 생성한 후 주입해준다.

이처럼 객체 간의 관계를 외부(Spring)가 설정해주는 것을 DI라고 한다.

---

## 4. 전체 흐름

1. Spring이 객체를 생성한다 → Bean  
2. 객체 생성과 관리의 주체가 Spring이다 → IoC  
3. 객체 간의 의존관계를 Spring이 연결한다 → DI
