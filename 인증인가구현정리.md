# Spring Security 인증/인가 구현 프로젝트 보고서

## 1. 프로젝트 개요
Spring Boot 환경에서 **Spring Security**를 활용하여 웹 애플리케이션의 기본적인 보안 체계(인증 및 인가)를 구축함.

## 2. 주요 구현 내용

### 1) 의존성 설정 (Dependencies)
`build.gradle`에 Spring Security 라이브러리를 추가하여 프로젝트 전체에 보안 필터 체인을 활성화함.
- `spring-boot-starter-security`: 보안의 핵심 기능을 제공.
- `spring-boot-starter-web`: 테스트용 REST API 구동을 위함.

### 2) 보안 설정 클래스 (SecurityConfig)
`@Configuration`과 `@EnableWebSecurity` 어노테이션을 사용하여 커스텀 보안 설정을 정의함.

- **인가(Authorization) 설정**: 
  - `/public/**` 경로는 `permitAll()`을 통해 로그인 없이 접근 가능하도록 허용.
  - 그 외의 모든 요청(`anyRequest()`)은 `authenticated()`를 통해 인증된 사용자만 접근 가능하도록 제한.
- **인증(Authentication) 설정**:
  - `formLogin()`을 활성화하여 Spring Security가 제공하는 기본 로그인 UI 사용.
  - 로그인 성공 시 이동할 기본 URL을 `/main`으로 지정.

### 3) 사용자 인증 데이터 관리 (Memory 기반)
테스트 편의를 위해 DB 연결 대신 메모리 내에 사용자 정보를 저장하는 `UserDetailsService`를 구현함.
- **아이디**: `test`
- **비밀번호**: `1234` (테스트를 위해 기본 인코더 사용)



## 3. 동작 프로세스 분석

1. **사용자 요청**: 사용자가 `/main` 경로로 접속 시도.
2. **필터 검문**: `SecurityFilterChain`이 해당 요청을 가로채서 인가 여부를 확인.
3. **인증 리다이렉트**: 인증되지 않은 사용자이므로 자동으로 `/login` 페이지로 이동.
4. **ID/PW 검증**: 사용자가 입력한 값과 메모리에 저장된 `test/1234` 정보를 비교.
5. **접근 허용**: 인증 성공 시 세션이 생성되며, 권한이 부여되어 `/main`의 컨트롤러 로직이 실행됨.
