# Flutter 주요 아키텍처 정리

## 1. 아키텍처(Architecture)

- 애플리케이션의 코드와 기능을 어떤 구조로 구성할지 정하는 방식
- 각 기능의 역할과 책임을 분리하기 위해 사용
- 프로젝트의 규모가 커질수록 중요도가 높아짐
- 유지보수성, 확장성, 테스트 용이성 등을 고려하여 선택함

---

## 2. MVC

- Model, View, Controller로 구성되는 아키텍처 패턴
- 데이터를 Model에서 관리하고 화면을 View에서 관리함
- Controller에서 사용자 입력과 프로그램의 흐름을 처리함
- 구조가 비교적 단순하여 작은 프로젝트에 적합함
- 프로젝트가 커지면 Controller에 많은 코드가 집중될 수 있음

### Model

- 애플리케이션에서 사용하는 데이터와 데이터 처리 담당
- 데이터의 상태와 관련된 로직을 관리함

### View

- 사용자에게 보여지는 UI 담당
- 화면을 구성하고 사용자에게 데이터를 표시함

### Controller

- 사용자의 입력을 처리함
- Model과 View 사이에서 데이터를 전달함
- 애플리케이션의 흐름을 제어함

---

## 3. MVP

- Model, View, Presenter로 구성되는 아키텍처 패턴
- MVC의 Controller 역할을 Presenter가 담당하는 형태
- UI와 비즈니스 로직을 분리하는 것을 중요하게 생각함

### Model

- 데이터와 데이터 처리 담당

### View

- UI와 사용자 입력 담당
- Presenter로부터 전달받은 데이터를 화면에 표시함

### Presenter

- View에서 발생한 이벤트 처리
- Model에서 데이터를 가져옴
- 처리된 데이터를 View에 전달함
- View와 Model을 연결하는 역할을 함

---

## 4. MVVM

- Model, View, ViewModel로 구성되는 아키텍처 패턴
- UI와 비즈니스 로직을 분리하는 것을 목적으로 함
- ViewModel에서 화면에 필요한 상태와 로직을 관리함
- Flutter의 상태 관리 라이브러리와 함께 사용할 수 있음

### Model

- 애플리케이션에서 사용하는 데이터 담당
- 데이터의 구조와 관련된 로직을 관리함

### View

- 사용자에게 보여지는 UI 담당
- 사용자의 입력을 받아 ViewModel에 전달함
- ViewModel의 상태를 화면에 표시함

### ViewModel

- View에 필요한 상태를 관리함
- 사용자 입력에 따른 로직을 처리함
- Model 또는 Repository에서 데이터를 가져옴
- View와 데이터 처리 로직을 분리함

---

## 5. MVI

- Model, View, Intent로 구성되는 아키텍처 패턴
- 단방향 데이터 흐름을 중요하게 생각함
- 사용자의 Intent에 따라 상태가 변경되고 변경된 상태가 View에 전달됨
- 상태 변화를 추적하기 쉬움
- 상태 관리가 복잡한 애플리케이션에서 활용할 수 있음

### Model

- 애플리케이션의 상태를 관리함

### View

- 현재 상태를 화면에 표시함
- 사용자의 입력을 Intent로 전달함

### Intent

- 사용자의 행동이나 이벤트를 표현함
- 상태 변경을 발생시키는 역할을 함

---

## 6. Layered Architecture

- 애플리케이션을 역할에 따라 여러 계층으로 나누는 구조
- 각 계층이 특정 역할을 담당하도록 구성함
- 계층별 책임을 분리하여 코드를 관리하기 쉽게 만듦

### Presentation Layer

- UI와 사용자 입력 담당
- 사용자에게 데이터를 보여주는 역할

### Business Layer

- 애플리케이션의 비즈니스 로직 담당
- 데이터 처리와 관련된 핵심 로직을 관리함

### Data Layer

- 데이터의 조회와 저장 담당
- API, 데이터베이스, 로컬 저장소 등과 연결됨

---

## 7. Clean Architecture

- 애플리케이션을 여러 계층으로 분리하는 아키텍처
- 각 계층의 책임을 명확하게 분리함
- 핵심 비즈니스 로직이 외부 기술에 의존하지 않도록 구성함
- 유지보수성과 테스트에 유리함
- 대규모 프로젝트에서 많이 사용됨

### Presentation Layer

- UI와 사용자 입력 담당
- View, ViewModel, 상태 관리 등이 포함될 수 있음

### Domain Layer

- 애플리케이션의 핵심 비즈니스 로직 담당
- Entity와 Use Case 등이 포함됨
- 외부 기술에 대한 의존성을 최소화함

### Data Layer

- 실제 데이터를 가져오고 저장하는 역할
- Repository 구현체와 Data Source 등이 포함됨
- API, 데이터베이스, 로컬 저장소 등과 연결됨

---

## 8. Feature-Based Architecture

- 기능을 기준으로 코드를 분리하는 구조
- 같은 기능에 필요한 코드를 하나의 영역에 모음
- 프로젝트 규모가 커졌을 때 기능을 찾고 관리하기 쉬움
- Flutter 프로젝트에서 자주 사용되는 구조

---

## 9. Clean Architecture + Feature-Based Architecture

- Clean Architecture와 Feature-Based Architecture를 함께 사용하는 방식
- 기능별로 폴더를 나누고 각 기능 내부에서 계층을 분리함
- 대규모 Flutter 프로젝트에서 활용하기 좋음

---

# 주요 설계 패턴

## 10. Repository Pattern

- 데이터 접근 로직을 Repository에서 관리하는 패턴
- ViewModel이나 비즈니스 로직이 데이터 저장 방식에 직접 의존하지 않도록 함
- API, 데이터베이스, 로컬 저장소 등의 데이터 접근 방식을 추상화함
- 데이터 접근 로직을 한 곳에서 관리할 수 있음
- MVVM과 Clean Architecture에서 자주 사용됨

---

## 11. Service Layer

- 외부 시스템과 통신하는 로직을 별도의 Service로 분리하는 구조
- API 요청과 네트워크 통신 등을 담당함
- 네트워크 통신과 비즈니스 로직을 분리할 수 있음
- HTTP 요청, 응답 처리 등의 로직을 관리할 수 있음

---

## 12. Use Case

- 애플리케이션에서 수행하는 하나의 비즈니스 기능을 독립적으로 분리한 것
- 주로 Domain Layer에서 사용함
- 비즈니스 로직을 작은 단위로 분리하여 관리할 수 있음
- Clean Architecture에서 중요한 개념

---

## 13. Dependency Injection

- DI라고도 함
- 객체가 필요한 의존성을 직접 생성하지 않고 외부에서 전달받는 방식
- 객체 간의 결합도를 낮출 수 있음
- 코드의 재사용성과 테스트 용이성을 높일 수 있음
- Riverpod과 같은 상태 관리 도구를 이용하여 구현할 수 있음

---

# 설계 원칙

## 14. 관심사의 분리

- 하나의 코드에 여러 역할을 넣지 않고 각각의 역할을 분리하는 개념
- UI, 상태 관리, 비즈니스 로직, 데이터 접근 등을 분리함
- 코드의 가독성과 유지보수성을 높일 수 있음

---

## 15. 단일 책임 원칙

- 하나의 클래스나 모듈은 하나의 책임을 가지도록 설계하는 원칙
- 하나의 클래스가 너무 많은 기능을 담당하지 않도록 함
- 코드 수정 시 다른 기능에 미치는 영향을 줄일 수 있음

---

## 16. 결합도

- 여러 코드가 서로 얼마나 강하게 연결되어 있는지를 나타내는 개념
- 결합도가 높으면 하나의 코드를 수정할 때 다른 코드에도 영향을 줄 가능성이 높음
- 의존성 분리와 추상화를 통해 결합도를 낮출 수 있음

---

## 17. 응집도

- 하나의 클래스나 모듈이 관련된 기능을 얼마나 잘 모아놓았는지를 나타내는 개념
- 하나의 모듈이 하나의 목적에 집중할수록 응집도가 높음
- 일반적으로 높은 응집도와 낮은 결합도를 목표로 함

---

# SOLID

## 18. SOLID

- 객체지향 프로그래밍에서 사용하는 5가지 설계 원칙
- 유지보수하기 쉽고 확장하기 좋은 코드를 작성하기 위해 사용함

### S - Single Responsibility Principle

- 하나의 클래스는 하나의 책임만 가져야 함

### O - Open/Closed Principle

- 확장에는 열려 있고 수정에는 닫혀 있어야 함

### L - Liskov Substitution Principle

- 자식 클래스는 부모 클래스를 대체할 수 있어야 함

### I - Interface Segregation Principle

- 사용하지 않는 기능에 의존하지 않도록 인터페이스를 분리해야 함

### D - Dependency Inversion Principle

- 구체적인 구현보다 추상화에 의존해야 함

---

# Flutter 상태 관리

## 19. 상태 관리

- 애플리케이션에서 발생하는 상태를 관리하는 방법
- 상태가 변경되었을 때 UI를 적절하게 갱신하기 위해 사용함
- 프로젝트의 규모와 구조에 따라 적절한 상태 관리 방법을 선택함

### 대표적인 상태 관리

- setState
- Provider
- Riverpod
- Bloc
- Cubit
- GetX

---

## 20. Riverpod

- Flutter에서 사용하는 상태 관리 및 의존성 관리 라이브러리
- Provider의 제한사항을 개선하기 위해 만들어짐
- 위젯 트리와 독립적으로 상태를 관리할 수 있음
- 컴파일 타임에 Provider 관련 오류를 확인할 수 있음
- 상태 관리와 Dependency Injection에 활용할 수 있음

---

# Flutter 개발에서 함께 알아두면 좋은 개념

## 21. API

- 서로 다른 프로그램이나 시스템이 데이터를 주고받기 위한 인터페이스
- Flutter에서는 서버와 데이터를 주고받을 때 주로 사용함
- REST API가 많이 사용됨

---

## 22. REST API

- HTTP를 기반으로 데이터를 주고받는 API 설계 방식
- HTTP 메서드를 이용하여 데이터에 대한 작업을 표현함
- 주요 HTTP 메서드
  - GET
  - POST
  - PUT
  - PATCH
  - DELETE

---

## 23. JSON

- 데이터를 저장하고 전달하기 위한 형식
- API 통신에서 많이 사용됨
- Key와 Value 형태로 데이터를 표현함
- Flutter에서는 JSON 데이터를 Dart 객체로 변환하여 사용하는 경우가 많음

---

## 24. 비동기 프로그래밍

- 작업이 완료될 때까지 프로그램 전체가 기다리지 않고 다른 작업을 수행할 수 있도록 하는 방식
- 네트워크 통신이나 파일 처리처럼 시간이 걸리는 작업에서 주로 사용함
- Dart에서는 Future, async, await 등을 사용함

---

## 25. Exception Handling

- 프로그램 실행 중 발생할 수 있는 오류를 처리하는 과정
- API 통신 오류, 데이터 변환 오류, 네트워크 오류 등을 처리함
- Dart에서는 try, catch, finally 등을 사용함
- 오류가 발생했을 때 애플리케이션이 정상적으로 대응할 수 있도록 함

---