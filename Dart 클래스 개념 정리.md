# Dart 클래스 개념 정리

## 1. 클래스 (Class)

객체를 만들기 위한 설계도이다.

클래스 안에는 데이터와 기능을 정의할 수 있다.

---

## 2. 객체 (Object)

클래스를 기반으로 실제로 만들어진 대상을 객체라고 한다.

하나의 클래스를 이용해 여러 개의 객체를 만들 수 있다.

---

## 3. 필드 (Field)

클래스 안에 선언된 변수이다.

객체가 가지고 있는 데이터를 표현한다.

예시:
- 이름
- 나이
- 점수
- 체력

---

## 4. 메서드 (Method)

클래스 안에 정의된 함수이다.

객체가 수행할 수 있는 기능을 표현한다.

예시:
- 공부하기
- 공격하기
- 이동하기
- 출력하기

---

## 5. 생성자 (Constructor)

객체가 생성될 때 실행되는 특별한 함수이다.

객체의 초기값을 설정할 때 주로 사용한다.

### 특징

- 클래스와 이름이 같다.
- 객체 생성 시 자동으로 실행된다.
- 매개변수를 통해 초기값을 전달할 수 있다.

---

## 6. this

현재 객체 자신을 가리킨다.

주로 생성자에서 매개변수와 필드의 이름이 같을 때 사용한다.

---

## 7. Named Constructor

생성자에 이름을 붙이는 방식이다.

하나의 클래스에서 여러 종류의 생성자를 만들 때 사용할 수 있다.

예시:
- 기본 생성자
- 관리자용 생성자
- 기본값을 사용하는 생성자

---

## 8. Named Parameter

매개변수의 이름을 지정해서 값을 전달하는 방식이다.

Flutter에서 매우 자주 사용된다.

### 장점

- 어떤 값인지 쉽게 알 수 있다.
- 매개변수의 순서를 외우기 쉽다.
- 코드의 가독성이 높아진다.

---

## 9. Getter

클래스의 값을 가져올 때 사용하는 기능이다.

특정 값을 그대로 반환하거나 계산된 값을 반환할 수 있다.

---

## 10. Setter

클래스의 값을 변경할 때 사용하는 기능이다.

값을 변경하기 전에 특정 조건을 검사하는 용도로도 사용할 수 있다.

---

## 11. static

객체가 아닌 클래스 자체에 속하는 멤버이다.

객체를 생성하지 않고 클래스 이름을 통해 사용할 수 있다.

주로 공통으로 사용하는 변수나 기능에 사용한다.

---

## 12. 상속 (Inheritance)

기존 클래스의 기능을 새로운 클래스가 물려받는 것이다.

### 부모 클래스

기능을 제공하는 클래스

### 자식 클래스

부모 클래스의 기능을 물려받는 클래스

### 장점

기존 코드를 재사용할 수 있다.

---

## 13. extends

부모 클래스를 상속할 때 사용한다.

자식 클래스는 부모 클래스의 기능을 물려받을 수 있다.

---

## 14. Override

부모 클래스에서 정의된 메서드를 자식 클래스에서 다시 정의하는 것이다.

자식 클래스의 상황에 맞게 부모의 기능을 변경할 수 있다.

---

## 15. super

부모 클래스에 접근할 때 사용한다.

주로 다음과 같은 경우에 사용한다.

- 부모 클래스의 생성자 호출
- 부모 클래스의 메서드 호출
- 부모 클래스의 필드 접근

---

## 16. abstract

추상 클래스를 만들 때 사용한다.

추상 클래스는 직접 객체를 생성하기보다는 자식 클래스에서 기능을 구현하도록 설계할 때 사용한다.

---

## 17. implements

인터페이스를 구현할 때 사용한다.

extends와 달리 부모 클래스의 구현을 그대로 물려받는 것이 아니라 필요한 기능을 직접 구현해야 한다.

---

# extends와 implements 차이

| 구분 | extends | implements |
|---|---|---|
| 목적 | 상속 | 인터페이스 구현 |
| 부모 기능 | 물려받음 | 직접 구현 |
| 사용 대상 | 클래스 | 클래스 |
| 특징 | 기존 기능 재사용 | 규칙에 맞게 기능 구현 |

---

# 클래스의 기본 구조

클래스는 다음과 같은 구조로 이해하면 된다.

클래스
→ 필드
→ 생성자
→ 메서드

---

# 상속 관련 핵심 개념

extends
→ 상속

implements
→ 인터페이스 구현

override
→ 부모 기능 재정의

super
→ 부모 클래스 접근

this
→ 현재 객체 접근

---

# 핵심 정리

| 개념 | 의미 |
|---|---|
| 클래스 | 객체를 만들기 위한 설계도 |
| 객체 | 클래스를 통해 만들어진 실제 대상 |
| 필드 | 객체가 가지고 있는 데이터 |
| 메서드 | 객체가 수행하는 기능 |
| 생성자 | 객체 생성 시 실행되는 함수 |
| this | 현재 객체 |
| static | 클래스 자체에 속하는 멤버 |
| extends | 상속 |
| implements | 인터페이스 구현 |
| override | 부모 기능 재정의 |
| super | 부모 클래스 접근 |
| abstract | 추상 클래스 |