Node.js + MySQL로 서버 구축은 백엔드 기본 조합이다

1.전체 구조
[클라이언트] -> [Node.js 서버] -> [My SQL DB]
Node.js: 서버 역할 (요청처리)
MySQL: 데이터 저장(DB)
둘을 연결해서 API 만들기

2.기본 준비
설치해야 할 것들
Node.js
MySQL
MySQL Workbench(선택)

3.프로젝트 생성

mkdir my-server
cd my-server
npm init -y

패키지 설치:
npm install express mysql2 cors
express:서버 쉽게 만드는 프레임 워크
mysql2:MySQL 연결
cors:프론트랑 연결할 때 필요

4. 기본 서버 코드

const express = require('express');
const app = express();

app.use(express.json());

app.get('/',(rep,res) =>{
      res.send('서버 정상 작동");
});

app.listen(3000,()=>{
     console.log('서버 실행됨');
});

실행:
node index.js

5.MySQL 연결

const mysql1 = require('mysql2');

const db = mtsql.createConnection({
      host: 'localhost',
      user: 'root',
      password: '비밀번호',
      database: 'testdb'
});

db.connect((err) => {
    if (err) {
       console.error('DB 연결 실패:,err);
    } else {
       console.log('DB 연결 성공');
   }
});

6.테이블 만들기 (예시)

CREATE TABLE users (
       id INT AUTO_INCREMENT PRIMARY KEY,
       name VARCHAR(50),
       age INT
);

7. CRUD API 만들기

1) 데이터 조회 (GET)

app.get('/users', (req, res) => {
     db.query('SELECT * FROM users' , (err, result) => {
         if (err) throw err;
         res.send(result);
     });
});

2) 데이터 추가 (POST)

app.post('/users', (req, res) => {
    const { name, age } = req.body;

    db.query(
        'INSERT INTO users (name, age) VALUES (?, ?)',
        [name, age],
        (err, result) => {
            if (err) throw err;
            res.send('추가 완료');
        }
    );
});
3)삭제(DELETE)
app.delete('/users/:id', (req, res) => {
    const id = req.params.id;

    db.query(
        'DELETE FROM users WHERE id = ?',
        [id],
        (err, result) => {
            if (err) throw err;
            res.send('삭제 완료');
        }
    );
});

8.테스트
Postman으로 API 요청 테스트
GET/users
POST/users
DELETE/users/:id

9.전체 흐름
1.클라이언트 요청
2.Node.js 서버 처리
3.mySQL 쿼리 실행
4.결과 반환

핵심요약
Node.js는 요청을 처리하고
MySQL은 데이터를 저장하며
두을 연결해 API 서버를 구축한다







